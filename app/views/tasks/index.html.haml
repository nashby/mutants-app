%h2 Tasks

= link_to 'New task', new_task_path

%table.table.table-hover
  %thead
    %tr
      %th #
      %th Name
      %th Team
      %th Actions

  %tbody
    - @tasks.each_with_index do |task, i|
      %tr
        %th= i + 1
        %td= link_to task.name, task
        %td
          - if task.team
            = link_to task.team.name, task.team
          - else
            = link_to 'Not assigned. Assign?', edit_task_path(task)

        %td
          = button_to 'Delete', task_path(task), method: :delete, class: 'btn btn-danger btn-xs'
